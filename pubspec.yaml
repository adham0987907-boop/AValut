name: avault
description: Secure Image Vault
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  image_picker: ^1.0.7
  local_auth: ^2.1.7
  path_provider: ^2.1.3
  permission_handler: ^11.3.3
  encrypt: ^5.0.3
  photo_manager: ^3.0.0

flutter:
  uses-material-design: true
